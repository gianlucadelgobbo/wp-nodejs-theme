//.boxItem(itemscope='', itemprop="itemListElement")
  .Exhibition(itemscope='', itemtype='http://schema.org/Event')
    .thumb-cnt
      a(itemprop='url', href='/exhibitions/'+result.slug+"/", rel='bookmark', title!=result.title.rendered)
        img.img-responsive(src=result.featured.thumbnail, alt!='Image for: '+result.title.rendered, itemprop='image', itemscope='', itemtype='https://schema.org/ImageObject')
    .box-list
      .box-list-cnt
        .text-muted
          include ../../_common/inc_eventdate
        h3.list_title(itemprop='name')
          a(itemprop='url', href='/exhibitions/'+result.slug+"/", rel='bookmark', title=result.title.rendered)!=result.title.rendered
        .places
          include ../../_common/inc_place
        .text-muted.small.excerpt
          if result.excerpt && result.excerpt.rendered
            div(itemprop='headline')
              p!=result.excerpt.rendered
          span.sr-only(itemprop='organizer')
.boxItem(itemprop="itemListElement", itemscope, itemtype="http://schema.org/ListItem")
  meta(itemprop="position", content=position+1)
  .Exhibition(itemprop="item", itemscope, itemtype='http://schema.org/Event')
    .thumb-cnt(itemprop='image', itemscope, itemtype="http://schema.org/ImageObject")
      a(href=lang_preurl+'/exhibitions/'+result.slug+"/", rel='bookmark', title!=result.post_title)
        img.img-responsive(src=result.featured.thumbnail, alt!='Image for: '+result.post_title, itemprop='url')
        meta(itemprop="width" content="480")
        meta(itemprop="height" content="270")
    .box-list
      .box-list-cnt
        .text-muted
          include ../../_common/inc_eventdate
        h3.list_title(itemprop='name')
          a(itemprop='url', href=lang_preurl+'/exhibitions/'+result.slug+"/", rel='bookmark', title=result.post_title)!=result.post_title
        .places
          include ../../_common/inc_place
        .text-muted.small.excerpt
          if result.excerpt && result.excerpt.rendered
            div(itemprop='description')
              p!=result.excerpt.rendered
          span.sr-only(itemprop='organizer')
