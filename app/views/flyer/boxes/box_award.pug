.boxItem(itemscope, itemprop="itemListElement", itemtype='http://schema.org/CreativeWork')
  .Award
    .text-center.boxItem
      a.ajaxloader(itemprop='url', href=lang_preurl+'/portfolio/awards-and-grants/' + result.slug + "/", rel='bookmark', title!=result.post_title)
        img(src=result.featured.full, alt!=global.__('Image for')+': '+result.post_title, itemprop='contentUrl')
      a.ajaxloader(itemprop='url', href=lang_preurl+'/portfolio/awards-and-grants/' + result.slug + "/", rel='bookmark', title!=result.post_title)
        h3!=result.post_title.replace(" | ", "<br />").replace(" | ", "<br />")
        meta(itemprop="position" content=position+1)
      !=result.excerpt.rendered
