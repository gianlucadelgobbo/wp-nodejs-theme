extends layout
block content
  .container-fluid
    article(itemscope, itemtype='http://schema.org/'+itemtype)
      header
        h1.entry-title(itemprop='name')!=data.post_title
      .row
        .col-sm-6.col-sm-offset-3
          script.
            var cx = "#{meta_data.meta.google_search}";
          form#cse-search-box(action='#')
            input(type='hidden', name='cx', value=meta_data.meta.google_search)
            input(type='hidden', name='cof', value='FORID:10')
            input(type='hidden', name='ie', value='UTF-8')
            .input-group
              input.form-control(type='text', name='q', size='31',value=q)
              span.input-group-btn
                button.btn(type='submit', name='sa') Search
      .rientro.searchresults
