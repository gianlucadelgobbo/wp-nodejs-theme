span.sr-only
    if result.startdateHR
        span(itemprop='startDate', content=result.startdateISO)=result.startdateHR
        !=" | "
    if result.enddateHR
        span(itemprop='endDate', content=result.enddateISO)=result.enddateHR
if result.data_evento
    .date
        span.fa.fa-calendar-check-o
        !=" "+result.data_evento