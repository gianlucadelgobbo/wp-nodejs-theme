if result && result.tags
  - tags = result.tags
if posttype && posttype.tags
  - tags = posttype.tags
if tags
  p.tags
    each tag, index in tags
      span.tagandcount!="#" + tag.name + " (" + tag.count + ")"
      if index + 1 < tags.length
        !=" · "
