if config.meta.editions[sessions.current_edition]['wpcf-location']
  .places
    //- //console.log("--------------------")
    - //console.log(config.meta.editions[sessions.current_edition]['wpcf-location'])
    each val, country in config.meta.editions[sessions.current_edition]['wpcf-location']
        each valcity, city in config.meta.editions[sessions.current_edition]['wpcf-location'][country]
          each val, venue in config.meta.editions[sessions.current_edition]['wpcf-location'][country][city]
            span.place(itemprop='location', itemscope, itemtype='http://schema.org/Place')
              if venue
                span.placeName(itemprop='name')=venue
                !=", "
              span(itemprop='geo', itemscope, itemtype='http://schema.org/GeoCoordinates')
                meta(itemprop='latitude', content=val.lng)
                meta(itemprop='longitude', content=val.lat)
              span.hide(itemprop='address', itemscope, itemtype='http://schema.org/PostalAddress')
                span(itemprop='addressLocality')=city
                !=" "
                span.addressCountry(itemprop='addressCountry')=country
                !=", "
          span.addressLocality=city
            !=", "
        span.addressCountry=country
